name: no_sus
description: "NO SUS — Secure study group workspace with file sharing, watermarking, and audit logging."
publish_to: 'none'

version: 1.1.0+6

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  smooth_page_indicator: ^2.0.1
  flutter_riverpod: ^3.3.1
  supabase_flutter: ^2.6.0
  app_links: ^6.0.0
  file_picker: ^11.0.2
  shared_preferences: ^2.2.0
  uuid: ^4.5.3
  pdfrx: ^2.4.4
  screen_protector: ^1.5.2
  share_plus: ^12.0.2
  http: ^1.2.0
  universal_html: ^2.3.0
  encrypt: ^5.0.3
  rive: ^0.13.20
  collection: ^1.19.0
  package_info_plus: ^9.0.1
  image: ^4.8.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1
  mocktail: ^1.0.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icon/LuxandNox.png
    - assets/mascot/
    # Note: the rest of assets/icon/ (app_icon.png etc.) is used by
    # flutter_launcher_icons at build time but is NOT included here as a
    # runtime asset (reduces APK size).

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/app_icon.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/icon/adaptive_foreground.png"
  adaptive_icon_monochrome: "assets/icon/adaptive_monochrome.png"

